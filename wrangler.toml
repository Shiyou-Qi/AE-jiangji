name = "ae-jiangji"
compatibility_date = "2026-09-09"
pages_build_output_dir = "webapp/out"

# This repository is a Cloudflare Pages static export.
# Use `wrangler pages deploy webapp/out --project-name=ae-jiangji`.
# If Cloudflare still runs `wrangler deploy`, the assets fallback below
# points Wrangler at the generated static export directory.

[assets]
directory = "webapp/out"
